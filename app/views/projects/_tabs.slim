ul.tabs
  - @categories.each do |category|
    li
    - if category.id == @active
      = link_to category.name, work_path + '/' + category.id, class: :active, remote: true
    - else
      = link_to category.name, work_path + '/' + category.id, remote: true
